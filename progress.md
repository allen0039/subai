# 进度记录

## 2026-09-12

- 汇总会话确认的产品需求及审计模块决策。
- 项目目录未发现已有产品代码或 Markdown 文档。
- 创建实施计划及调研记录；尚未开发、调用真实审核 API 或部署。
- 已检查主文档章节、关键需求和代码围栏完整性；检查通过。
- 计划编制完成；产品开发、真实API验证和部署仍未开始。

## v0.2 详细实施规格

- 主计划扩展至24节，补齐动态主体、表字段、接口、金额精度、预算周期、状态机、默认规则目录、配置和证据要求。
- 新增DEVELOPMENT_HANDOFF.md，包含实施顺序、交付清单和审查报告模板。
- 验证章节连续、围栏闭合、固定五设备表述已移除、交接文件存在；通过。
- 所有产品功能仍属于待实施，未进行真实账号或部署操作。

## 2026-09-12 第一版实施

- 依据 IMPLEMENTATION_PLAN.md v0.2 与 DEVELOPMENT_HANDOFF.md 完成第一版开发。
- 交付：Go 单体网关（cmd/server + internal/*）、PostgreSQL 迁移、默认规则包与正反例、
  React 管理界面、Compose/Caddyfile/.env.example/Makefile、docs 全套（API/兼容矩阵/
  费用上界/审核容量/价格源/决策/限制/状态/交接报告）。
- 测试：单元测试通过；13 项集成测试通过（合成上游/合成审核 + 真实 PostgreSQL）；
  端到端实测：管理 API、数据面错误映射、单活锁、未就绪门、浏览器界面。
- 真实联调（P0-01..04）因缺少真实凭证全部 blocked，未伪造任何真实兼容证据。
- 遗留：审计日志清理 job 未实现；OpenAPI 机器可读文件未生成；压测脚本未交付；
  详见 docs/REVIEW_HANDOFF.md 第 6 节。
- 错误记录：无未解决的构建或测试失败。

## 2026-09-12 独立审查修复

- 依据 docs/INDEPENDENT_REVIEW_2026-09-12.md 完成 8 项 P1 与 6 项 P2 修复，逐项对照见 docs/REVIEW_FIXES_2026-09-12.md。
- 新增 7 项集成回归（断连结算、对账资金、fallback 零重放、未验证价格零调用、缺失 flagged 字段、主路由 OAuth 回调、unknown 处置、调度满载跳过）+ 4 项 SSE 单测；集成测试总计 20 项通过。
- Docker 完整镜像构建与内容验证通过；端到端冒烟确认统一 readiness、OAuth 回调路由、单活锁。
- 剩余未项：审计日志清理任务、压测脚本、OpenAPI 文件、真实联调（P0）。

## 2026-09-12 第二轮独立审查修复

- 依据 INDEPENDENT_REVIEW_ROUND2_2026-09-12.md 修复全部 6 项 P1 与 4 项 P2，逐项对照见 docs/REVIEW_FIXES_ROUND2_2026-09-12.md。
- 关键修复：出口错误三分类（拨号/已接收未响应/已开始流）终结重放歧义；超预留人工调整保持账号隔离；unknown 调整改用请求冻结价格版本；启动恢复事务化可重入并补齐半收敛状态；上游执行与收尾 context 分离且超时可配置；Compose env_file 注入生产开关。
- 回归：新增 8 项集成测试 + 2 项队列单测，断连测试改为同步事件驱动；29 项顶层集成测试全部通过。
- 更正第一轮记录的两处证据表述（测试计数、TestMain skip 语义），补齐管理写接口的缓存失效接线与锁监控探测超时。

## 2026-09-14 WebUI 基线修复与计划

- 完成全面审查：发现前端原型无法 TypeScript 构建、覆盖正式登录/权限/哈希路由、包含不存在 API 与静态演示数据。
- 恢复正式入口并完成首轮安全改造：分组导航、可折叠侧栏、主题记忆、真实状态快照卡、弹窗 Esc/焦点恢复。
- 验证：`npm ci && npm run build` 通过；`go test ./...` 通过；`git diff --check` 通过。
- 本地浏览器自动化未执行：环境缺少 Python Playwright 模块；未将临时测试脚本保留在仓库。
- 创建 `docs/WEBUI_REFACTOR_REPAIR_PLAN.md`；下一步为 P0 原型收敛与文档一致性修复，尚未部署或发布。

## 2026-09-14 WebUI 全量实施与验收

- 完成 P0：移除未接入正式入口的原型目录、静态演示内容与失实完成说明。
- 完成 P1/P2：在原有 hash 路由和认证/权限逻辑之上加入控制塔 Shell、分组导航、折叠侧栏、主题记忆、筛选、请求取消、空态和弹窗 Esc/焦点恢复。
- 完成 P3/P4：新增受会话权限保护的仪表盘、全局搜索、事件流与个人概览 API；新增时间索引迁移；账号列表支持标签前缀和状态筛选；前端不再汇总全量分页数据或回退到模拟数据。
- 完成 P5：个人台展示订阅 → Key → 资源访问链路和本人近 7 日已记账成本，同时明确不展示上游账号、出口与代理细节。
- 新增 WebUI 端点集成验收，并修复测试数据库清理对 `account_quota_snapshots` 的遗漏。验证已通过：`go test ./...`、`npm run build`、`make test-integration`（真实 Docker PostgreSQL）。
- 未发布、未推送、未更新 Oracle3；接下来执行发布前 lint、镜像构建和部署前检查。

## 2026-09-14 双主题色彩规划

- 基于用户提供的登录页和管理页截图，以及当前 CSS 源码，定位浅色模式的硬编码深色控件与 token 重复定义问题。
- 新建 `docs/WEBUI_THEME_PLAN.md`，提供深/浅色 token、五阶段迁移与可访问性验收方案。
- 本轮仅输出计划，未修改运行时代码、未触发构建或发布。

## 2026-09-14 双主题色彩实施

- 用单一语义 token 系统替换重复主题定义和通用组件中的硬编码深色值；覆盖页面背景、导航、表格、表单、按钮、状态反馈、搜索、配额弹窗和个人台。
- 增加登录页主题开关；主题首次加载直接读取系统/已保存偏好，避免刷新时覆盖选择。
- 浏览器视觉检查已确认登录页深色、浅色及刷新后持久化；`npm run build`、`go test ./...`、`make test-integration`（Docker PostgreSQL）、`git diff --check` 均通过。
- 本轮未提交、未推送、未部署 Oracle3，等待用户决定是否发布主题改造。

## 2026-09-14 上游额度刷新与重置卡

- 已确认现有逐账号额度刷新端点、JSONB 快照存储和前端额度弹窗可复用。
- 已核对 Sub2API 使用的官方只读重置卡详情接口及最小展示字段；开始实现后端兼容采集。
- 本轮只实现查询与展示，不实现重置卡消耗，不提交、推送或部署。
- 后端已新增重置卡只读详情采集，支持数组及常见 envelope 字段，并仅持久化张数和过期时间；详情失败时保留 `/usage` 基础结果。
- 上游账号列表已新增逐行“刷新额度”，摘要显示重置卡张数；详情弹窗显示最近及全部过期时间，并在刷新后保持打开、原地更新。
- 验收通过：`go test ./...`、`npm run build`、`make test-integration`、`git diff --check`；内置浏览器完成深浅主题与交互检查，控制台无错误。

## 2026-09-14 模型能力与精细计费方案

- 使用 planning-with-files 工作流盘点现有实现并研究 Sub2API 的模型配置结构。
- 已确定项目已有 Key/订阅模型白名单、全局模型单价覆盖和套餐全局倍率；缺少池级能力、模型映射、模型级售价与模型管理界面。
- 已完成详细设计文档，方案保持 `price_version` 和 `plan_version` 双冻结，以免变更运营规则改写历史预算或账本。
- 设计复核后明确保留现有 `usage_ledger.base_cost` 作为目录采购成本，新增 `pricing_base_cost` 承载套餐模型规则后的销售基准，避免模型特价覆盖采购审计证据。
- 本轮只输出方案，不修改运行时代码、数据库或部署配置。

## 2026-09-14 模型能力与精细计费实施

- 用户已授权开始实施。新增 0014 迁移草案，包含账号 capability、账号池模型规则、套餐版本模型价格规则以及请求/账本审计字段。
- 调度器新增按公开模型筛选的 `AcquireModelRoute`：已配置 capability 的账号严格匹配，未配置 capability 的旧账号保留兼容行为；池规则同样在占槽前收紧候选。
- 数据面会用调度结果把出站 payload 顶层 `model` 改写为上游模型名，客户端模型名仍用于权限校验。
- 首次局部编译因未导入 `pgx.ErrNoRows` 失败；已记录并改为补充精确导入。
## 2026-09-14 模型能力与精细计费实施

- 已新增 `0014_model_capabilities_and_pricing.sql`：账号能力映射、账号池模型规则、套餐版本模型价格规则，以及请求/账本审计字段。
- 网关在调度前按公开模型筛选账号，按映射替换实际转发模型；未配置能力记录的旧账号保持兼容行为。
- 价格目录成本和套餐实际计费成本分离记账；未知结果人工结算沿用冻结套餐版本规则。
- 新增管理 API：账号模型能力、账号池模型规则、活跃价格目录；创建套餐/版本支持 `model_pricing` 并在套餐列表返回当前规则。
- 验证：`go test ./...` 通过，`git diff --check` 通过。

## 2026-09-14 sub2api 整体替换方案编制

- 用户要求具体实施方案，本轮仅调研和文档。
- 已读取 planning-with-files 技能，核对当前 OAuth 和发布文档，正在获取上游源码。

- 已完成 docs/SUB2API_REBASE_IMPLEMENTATION_PLAN.md，包含 P0–P5、接口边界、模块验收、数据分支、上线及含凭证轮换的回滚方案。
- 仅修改文档，未构建/测试上游产品、未迁移数据、未推送部署。

## 实施开始
完整上游已导入隔离 worktree；旧运行时代码由 Git 历史保存，无旧运行时双轨。未操作生产。

- 上游基线 GPT 相关四个包测试、前端 build 通过。本地 pnpm 11 无法使用上游锁文件，改用上游指定 pnpm 9，未改锁文件。
- 初轮适配检查发现旧 HTTP 客户端测试引用与 Vue narrowing 两处编译问题；已调整为新授权契约并修复。
- GPT 改用原 SubAI PKCE、10 分钟会话、标准 HTTP 表单、完整回调与身份提取；生产 Redis 会话失败不回退到进程内。

- 完整性清单核对 3919 个上游跟踪文件，缺失 0。
- 后端 unit 全包检查通过；前端 2098 项测试中 2 处测试夹具失败，已在未改上游复现，修正供应商数量断言与 Pinia 初始化，不修改对应产品功能。
- Docker 自有镜像成功构建；已启动仅本地测试使用的独立 PostgreSQL/Redis，准备空库启动验证。

- 完成全量前端 2098/2098、后端 unit/integration、GPT race 专项及 golangci-lint（0 issues）。
- 浏览器验证新登录页、账号管理、GPT 授权向导；正确提交完整回调并拒绝错误 state。真实 Redis 会话通过应用重启恢复验证。
- 完成 docs/subai/README.md、VALIDATION.md、完整性核对脚本与自有 Compose/发布配置。

- 最终镜像再次通过浏览器授权回填验证；测试专用容器/卷和临时凭证已清理，保留本地镜像和无敏感信息的检查记录。
- 工作区变更保持未提交，索引无已暂存内容；原 main 不受运行时代码替换影响。


## 2026-09-14 应用到 main
按用户要求，将已验证的隔离工作区源码应用到 main：4105 个变更路径逐项内容及权限核对一致。额外迁入 docs/subai 文档并修正忽略规则，保留 GPT 原登录方案；未提交、推送、部署或迁移旧数据库。此前 main 保留旧运行时代码的记录为历史状态。

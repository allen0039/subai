# 给实施工具的任务说明

请实现本目录 IMPLEMENTATION_PLAN.md v0.2 所定义的 SubAI 独立网关。该计划是需求与验收基准，不能只生成演示界面或占位后端。

## 工作范围

- 动态管理成员、客户端、Key、账号及账号组。三台电脑和两个Hermes仅是初期样本，不是产品上限。
- 独立Go后端、PostgreSQL、管理界面与Compose部署。
- Pro/Codex授权、每账号代理、固定美元预算和百分比、双层并发。
- 内置可编辑本地规则包、官方Moderation前置审核、限流队列与脱敏审计。
- 默认价格来自官方API定价，能否自动可靠解析须实测，不伪造同步功能。

## 执行顺序

1. 完整阅读主计划，建立docs/IMPLEMENTATION_STATUS.md，逐项列出P0-01至P6-02的状态。
2. 先做P0能力验证和模拟测试设施。没有真实凭证时可继续独立模块实现，真实联调明确not_run。
3. 按P1—P6实现，每阶段记录命令、测试结果、证据路径和限制。
4. 规格有冲突或实测证明不可行时，记录到docs/DECISIONS.md并给出选择；不能静默降低审核或预算约束。
5. 初步完工后生成docs/REVIEW_HANDOFF.md，供用户交给后续审查工具。

## 不可省略的交付

- 真实可运行源码、锁文件、数据库迁移、默认规则与正反例。
- docs/API或OpenAPI文件、兼容矩阵、费用上界报告、审核容量报告、价格源说明。
- .env.example、Compose、运行及备份恢复手册、测试执行入口。
- 已知限制、偏离清单、未验证项目及原因。

## 禁止用这些方式声称完成

- 五个固定设备卡片代替动态实体。
- 模拟响应代替真实兼容证据。
- 审核超时直接放行、只审最后一句却称完整审核。
- 请求完成后才扣账却称严格预算，或把缺失用量当0。
- 代理失败隐式直连，或未经明确配置自动换账号重放已发送请求。
- 硬编码凭证、把秘密提交到仓库、在报告贴完整密钥。

## 审查交接报告模板

1. 实现版本/提交与启动步骤。
2. 已实现范围、未实现范围、与规格偏离。
3. 测试命令、结果、真实联调与模拟测试的区别。
4. 审核前置、预算并发、出口隔离、重启恢复的证据。
5. 依赖或外部接口限制，以及需要用户提供的环境信息。
6. 后续审查建议优先检查的文件与未解决问题。

用户会在初步实现后进行独立审查；当前计划文档不是已通过审查的证明。
